/**
 * Main entry point for the Freshservice Change Management app
 * This file now redirects to app.js to avoid duplication of code.
 */

// For backward compatibility, include app.js which now handles all initialization
import './app.js';

console.log('index.js loaded - all initialization is now handled by app.js'); 